import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import Input from '../../base/Input'
import FormInput from './FormInput'
import SimpleInput from './SimpleInput'

<Meta title="Inputs" component={Input} />

# Inputs

## Standard inputs for forms and user input

Forms come in two forms. Ideally they are provided with `onChange` handlers.

<Preview withToolbar>
  <Story name="Standard">
    <div>
      <FormInput label="Username" placeholder="Rick John" />
      <FormInput password label="Password" />
      <SimpleInput placeholder="Simple Input" />
      <SimpleInput password />
    </div>
  </Story>
</Preview>

### Props

<Props of={Input} />
