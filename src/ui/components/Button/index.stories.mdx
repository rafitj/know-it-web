import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { Button } from '../../base/Button'
import { PrimaryButton } from './PrimaryButton'
import { SecondaryButton } from './SecondaryButton'
import { RedButton } from './RedButton'
import { BlueButton } from './BlueButton'

<Meta title="Buttons" component={Button} />

# Buttons

## Standard brand buttons

<Preview withToolbar>
  <Story name="Standard">
    <PrimaryButton text="Join now!" />
  </Story>
</Preview>

### Variations

Use only the provided composite variations:

<Preview withToolbar>
  <Story name="Variations">
    <div>
      <PrimaryButton text="Primary Button" />
      <SecondaryButton text="Secondary Button" />
      <RedButton text="Red Button" />
      <BlueButton text="Blue Button" />
    </div>
  </Story>
</Preview>

### AntD Props

Buttons permit a few powerful Ant Design states/properties but not all.

<Preview withToolbar>
  <Story name="Ant-Design">
    <div>
      <PrimaryButton loading text="Loading" />
      <BlueButton disabled text="Disabled Button" />
    </div>
  </Story>
</Preview>

### Props

<Props of={Button} />
